//! `pidge inbox show` — display a single message.

use anyhow::Result;

#[allow(dead_code)]
pub async fn run(
    _fragment: String,
    _mark_read: bool,
    _show_images: bool,
    _json: bool,
) -> Result<()> {
    unimplemented!("inbox show is implemented in a later task")
}
